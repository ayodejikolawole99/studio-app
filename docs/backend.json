{
  "entities": {
    "Employee": {
      "title": "Employee",
      "type": "object",
      "description": "Represents an employee in the canteen system.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Full name of the employee."
        },
        "employeeId": {
          "type": "string",
          "description": "The employee's official ID number, used as the document ID."
        },
        "department": {
          "type": "string",
          "description": "The department the employee belongs to."
        },
        "ticketBalance": {
            "type": "number",
            "description": "The number of meal tickets the employee currently has."
        },
        "hasBiometric": {
            "type": "boolean",
            "description": "Indicates if biometric data has been enrolled for this employee."
        }
      },
      "required": [
        "name",
        "employeeId",
        "department",
        "ticketBalance"
      ]
    },
    "FeedingRecord": {
      "title": "FeedingRecord",
      "type": "object",
      "description": "Represents a record of an employee's meal consumption.",
      "properties": {
        "employeeId": {
          "type": "string",
          "description": "The ID of the employee who consumed the meal."
        },
        "employeeName": {
            "type": "string",
            "description": "The name of the employee (denormalized for easy display)."
        },
        "department": {
            "type": "string",
            "description": "The department of the employee (denormalized)."
        },
        "timestamp": {
          "type": "string",
          "description": "The date and time of the meal consumption, stored as a Firestore Timestamp.",
          "format": "date-time"
        }
      },
      "required": [
        "employeeId",
        "employeeName",
        "department",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
        {
            "path": "/employees/{employeeId}",
            "definition": {
                "schema": {
                    "$ref": "#/entities/Employee"
                },
                "description": "Stores employee profiles. The document ID is the official employee number."
            }
        },
        {
            "path": "/feedingRecords/{recordId}",
            "definition": {
                "schema": {
                    "$ref": "#/entities/FeedingRecord"
                },
                "description": "A log of all feeding events. Data is denormalized for efficient querying and reporting."
            }
        }
    ]
  }
}
